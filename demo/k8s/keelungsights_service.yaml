apiVersion: v1
kind: Service
metadata:
  name: keelungsights-service
spec:
  type: NodePort
  selector:
    app: keelungsights
  ports:
  - protocol : TCP
    nodePort: 30808
    port: 8080
    targetPort: 8080 